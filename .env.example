# planovac / F0-07
# Tento soubor slouzi jako vychozi sablona pro lokalni konfiguraci.
# Pro lokalni vyvoj ho zkopirujte do .env.local a doplnte konkretni hodnoty.

# Povinna identifikace prostredi v aplikaci.
# Povolené hodnoty: local, preview, production
NEXT_PUBLIC_APP_ENV=local

# Zakladni verejna adresa aplikace pro odkazy, metadata a smoke overeni.
NEXT_PUBLIC_APP_BASE_URL=http://localhost:3000

# Verejne hodnoty pro Supabase baseline ve fazi F0-04.
# Konkretni hodnoty se lisi podle local / preview / production prostredi.
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Server-side secret pro operace, ktere nesmi bezet v klientovi.
# Nikdy ho nevkladejte do klientoveho kodu ani do verejnych promennych.
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Prefix pro preview schema ve sdilenem neprodukcnim Supabase projektu.
# Ocekavana konvence je preview_<pr-cislo>.
SUPABASE_PREVIEW_SCHEMA_PREFIX=preview_

# Adresa pouzita pro lokalni nebo post-deploy smoke kontroly.
SMOKE_BASE_URL=http://localhost:3000

# Callback path pro dokonceni prihlaseni pres Supabase Auth magic link.
SUPABASE_AUTH_REDIRECT_PATH=/auth/callback

# Poznamka:
# VERCEL_ENV, VERCEL_URL a VERCEL_GIT_COMMIT_SHA typicky dodava Vercel automaticky.
# Pro F0-06 je vhodne ve Vercelu zapnout "Automatically expose System Environment Variables",
# aby byly dostupne i NEXT_PUBLIC_VERCEL_TARGET_ENV, NEXT_PUBLIC_VERCEL_BRANCH_URL
# a NEXT_PUBLIC_VERCEL_PROJECT_PRODUCTION_URL pro diagnostiku preview a production deploye.
# GITHUB_SHA typicky dodava GitHub Actions automaticky.
